namespace Addins.Management.Commands
{
    /// <summary>
    /// 命令类型
    /// </summary>
    public enum CommandType
    {
        Start,
        Stop,
        Reload,
        Configure,
        Install,
        Uninstall
    }
}
