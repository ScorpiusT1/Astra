<UserControl x:Class="Astra.UI.Styles.Controls.BusyOverlayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:Astra.UI.Styles.Controls"
             d:DesignHeight="300"
             d:DesignWidth="420"
             mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <Style x:Key="BusyCancelButtonStyle"
               TargetType="Button">
            <Setter Property="MinWidth"
                    Value="108" />
            <Setter Property="Height"
                    Value="34" />
            <Setter Property="Padding"
                    Value="16,0" />
            <Setter Property="FontSize"
                    Value="13" />
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="Foreground"
                    Value="{DynamicResource ReverseTextBrush}" />
            <Setter Property="Background"
                    Value="{DynamicResource DangerBrush}" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter TargetName="Root"
                                        Property="Opacity"
                                        Value="0.9" />
                            </Trigger>
                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter TargetName="Root"
                                        Property="Opacity"
                                        Value="0.76" />
                            </Trigger>
                            <Trigger Property="IsEnabled"
                                     Value="False">
                                <Setter TargetName="Root"
                                        Property="Opacity"
                                        Value="0.45" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="BusyProgressBarStyle"
               TargetType="ProgressBar">
            <Setter Property="Minimum"
                    Value="0" />
            <Setter Property="Maximum"
                    Value="100" />
            <Setter Property="Value"
                    Value="0" />
            <Setter Property="Height"
                    Value="5" />
            <Setter Property="Background"
                    Value="{DynamicResource LightPrimaryBrush}" />
            <Setter Property="Foreground"
                    Value="{DynamicResource PrimaryBrush}" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsBusy, RelativeSource={RelativeSource AncestorType={x:Type local:BusyOverlayControl}}}"
                             Value="True">
                    <DataTrigger.EnterActions>
                        <BeginStoryboard Name="BusyProgressStoryboard">
                            <Storyboard RepeatBehavior="Forever">
                                <DoubleAnimation Storyboard.TargetProperty="Value"
                                                 From="0"
                                                 To="100"
                                                 Duration="0:0:1.2" />
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.EnterActions>
                    <DataTrigger.ExitActions>
                        <StopStoryboard BeginStoryboardName="BusyProgressStoryboard" />
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Value"
                                                 To="0"
                                                 Duration="0:0:0" />
                            </Storyboard>
                        </BeginStoryboard>
                    </DataTrigger.ExitActions>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Visibility="{Binding IsBusy, RelativeSource={RelativeSource AncestorType={x:Type local:BusyOverlayControl}}, Converter={StaticResource BoolToVisibilityConverter}}">
        <Border Background="{DynamicResource MaskBrush}" />

        <Border HorizontalAlignment="Center"
                VerticalAlignment="Center"
                MinWidth="320"
                MaxWidth="520"
                Margin="24"
                Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="14">
            <Border.Effect>
                <DropShadowEffect Color="{DynamicResource ShadowColor}"
                                  Opacity="0.2"
                                  ShadowDepth="2"
                                  BlurRadius="24"
                                  Direction="270" />
            </Border.Effect>

            <Grid>
                <Border Height="4"
                        VerticalAlignment="Top"
                        CornerRadius="14,14,0,0"
                        Margin="1,0,1,0"
                        Background="{DynamicResource PrimaryBrush}" />
                <StackPanel Margin="22,18,22,18">
                    <TextBlock Text="系统繁忙"
                               FontSize="16"
                               FontWeight="Bold"
                               Foreground="{DynamicResource TitleBrush}" />
                    <TextBlock Margin="0,8,0,0"
                               Text="{Binding BusyMessage, RelativeSource={RelativeSource AncestorType={x:Type local:BusyOverlayControl}}}"
                               FontSize="13"
                               LineHeight="20"
                               Foreground="{DynamicResource PrimaryTextBrush}"
                               TextWrapping="Wrap" />

                    <ProgressBar Margin="0,14,0,0"
                                 Style="{StaticResource BusyProgressBarStyle}" />

                    <TextBlock Margin="0,8,0,0"
                               Text="正在执行耗时操作，请稍候..."
                               FontSize="12"
                               Foreground="{DynamicResource SecondaryTextBrush}" />

                    <Button Margin="0,16,0,0"
                            HorizontalAlignment="Center"
                            Style="{StaticResource BusyCancelButtonStyle}"
                            Content="{Binding CancelButtonText, RelativeSource={RelativeSource AncestorType={x:Type local:BusyOverlayControl}}}"
                            Command="{Binding CancelCommand, RelativeSource={RelativeSource AncestorType={x:Type local:BusyOverlayControl}}}"
                            Visibility="{Binding ShowCancelButton, RelativeSource={RelativeSource AncestorType={x:Type local:BusyOverlayControl}}, Converter={StaticResource BoolToVisibilityConverter}}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
