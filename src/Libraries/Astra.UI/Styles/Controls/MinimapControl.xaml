<UserControl x:Class="Astra.UI.Styles.Controls.MinimapControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Astra.UI.Styles.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Canvas x:Name="MinimapCanvas" />
        <Rectangle x:Name="ViewportIndicator"
                   StrokeThickness="1.5"
                   Stroke="{DynamicResource BrushViewportStroke}"
                   Fill="{DynamicResource BrushViewportFill}"
                   IsHitTestVisible="True"
                   Panel.ZIndex="100" />
    </Grid>
</UserControl>
