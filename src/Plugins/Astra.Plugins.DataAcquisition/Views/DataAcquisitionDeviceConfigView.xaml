<UserControl x:Class="Astra.Plugins.DataAcquisition.Views.DataAcquisitionDeviceConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题 -->
            <TextBlock Grid.Row="0" Text="数据采集设备配置" 
                       FontSize="18" FontWeight="Bold" 
                       Margin="0,0,0,20"/>

            <!-- 设备名称 -->
            <Grid Grid.Row="1" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="设备名称:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" Text="{Binding DeviceName, UpdateSourceTrigger=PropertyChanged}" 
                         hc:InfoElement.Placeholder="请输入设备名称"/>
            </Grid>

            <!-- 序列号 -->
            <Grid Grid.Row="2" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="序列号:" VerticalAlignment="Center"/>
                <TextBox Grid.Column="1" Text="{Binding SerialNumber, UpdateSourceTrigger=PropertyChanged}" 
                         hc:InfoElement.Placeholder="请输入设备序列号"/>
            </Grid>

            <!-- 采样率 -->
            <Grid Grid.Row="3" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="采样率 (Hz):" VerticalAlignment="Center"/>
                <hc:NumericUpDown Grid.Column="1" Value="{Binding SampleRate}" 
                               Minimum="1" Maximum="1000000" 
                               Increment="1000"/>
            </Grid>

            <!-- 通道数量 -->
            <Grid Grid.Row="4" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="通道数量:" VerticalAlignment="Center"/>
                <hc:NumericUpDown Grid.Column="1" Value="{Binding ChannelCount}" 
                               Minimum="1" Maximum="128" 
                               Increment="1"/>
            </Grid>

            <!-- 缓冲区大小 -->
            <Grid Grid.Row="5" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="缓冲区大小:" VerticalAlignment="Center"/>
                <hc:NumericUpDown Grid.Column="1" Value="{Binding BufferSize}" 
                               Minimum="64" Maximum="1048576" 
                               Increment="1024"/>
            </Grid>

            <!-- 自动启动 -->
            <CheckBox Grid.Row="6" Content="插件启动时自动开始采集" 
                      IsChecked="{Binding AutoStart}" 
                      Margin="0,0,0,15"/>

            <!-- 启用状态 -->
            <CheckBox Grid.Row="7" Content="启用设备" 
                      IsChecked="{Binding IsEnabled}" 
                      Margin="0,0,0,15"/>

            <!-- 分组和槽位 -->
            <Grid Grid.Row="8" Margin="0,0,0,15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="分组ID:" Margin="0,0,0,5"/>
                    <TextBox Grid.Row="1" Text="{Binding GroupId, UpdateSourceTrigger=PropertyChanged}" 
                             hc:InfoElement.Placeholder="G0"/>
                </Grid>
                <Grid Grid.Column="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="槽位ID:" Margin="0,0,0,5"/>
                    <TextBox Grid.Row="1" Text="{Binding SlotId, UpdateSourceTrigger=PropertyChanged}" 
                             hc:InfoElement.Placeholder="S0"/>
                </Grid>
            </Grid>

            <!-- 设备ID（只读） -->
            <Grid Grid.Row="9">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="设备ID:" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding DeviceId}" 
                           VerticalAlignment="Center" 
                           Foreground="Gray" 
                           IsEnabled="False"/>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
