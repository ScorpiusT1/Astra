<Window x:Class="Astra.Views.HomeModules.TestItemChartWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:scottplot="http://schemas.scottplot.net/2008/xaml/presentation"
        Title="{Binding Name, StringFormat=测试项图表 - {0}}"
        Width="900"
        Height="600"
        MinWidth="700"
        MinHeight="420"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="16">
        <!-- 坐标轴标题由代码后置根据 ChartDisplayPayload 设置：底部/左侧各为「轴标签 + 单位」（单位为空则仅显示标签）。 -->
        <Border
                CornerRadius="10"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                Background="{DynamicResource RegionBrush}">
            <scottplot:WpfPlot x:Name="ItemPlot"
                               Margin="14" />
        </Border>
    </Grid>
</Window>
