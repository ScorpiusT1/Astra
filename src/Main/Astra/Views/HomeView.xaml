<UserControl x:Class="Astra.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:Astra.ViewModels"
             xmlns:moduleViews="clr-namespace:Astra.Views.HomeModules"
             d:DataContext="{d:DesignInstance Type=viewmodels:HomeViewModel}"
             mc:Ignorable="d"
             Background="{DynamicResource BackgroundBrush}"
             d:DesignHeight="720"
             d:DesignWidth="1280">
    <UserControl.Resources>
        <!-- Home 页面局部固定使用 Light 主题色 -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="#F5F5F5" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="RegionBrush" Color="#FFFFFF" />
        <SolidColorBrush x:Key="SecondaryRegionBrush" Color="#FAFAFA" />
        <SolidColorBrush x:Key="ThirdlyRegionBrush" Color="#F5F5F5" />

        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#212121" />
        <SolidColorBrush x:Key="SecondaryTextBrush" Color="#757575" />
        <SolidColorBrush x:Key="ThirdlyTextBrush" Color="#9E9E9E" />
        <SolidColorBrush x:Key="TitleBrush" Color="#1976D2" />

        <SolidColorBrush x:Key="BorderBrush" Color="#E0E0E0" />
        <SolidColorBrush x:Key="SecondaryBorderBrush" Color="#BDBDBD" />

        <SolidColorBrush x:Key="PrimaryBrush" Color="#2196F3" />
        <SolidColorBrush x:Key="DarkPrimaryBrush" Color="#1976D2" />
        <SolidColorBrush x:Key="SuccessBrush" Color="#4CAF50" />
        <SolidColorBrush x:Key="DangerBrush" Color="#F44336" />
        <SolidColorBrush x:Key="InfoBrush" Color="#00BCD4" />

        <SolidColorBrush x:Key="LightPrimaryBrush" Color="#E3F2FD" />
        <SolidColorBrush x:Key="LightSuccessBrush" Color="#E8F5E9" />
        <SolidColorBrush x:Key="LightDangerBrush" Color="#FFEBEE" />
        <SolidColorBrush x:Key="LightInfoBrush" Color="#E0F7FA" />
        <SolidColorBrush x:Key="LightWarningBrush" Color="#FFF3E0" />
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="360" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <moduleViews:YieldModuleView Grid.Row="0"
                                         DataContext="{Binding YieldModule}" />

            <moduleViews:IOMonitorModuleView Grid.Row="1"
                                             DataContext="{Binding IoMonitorModule}" />

            <moduleViews:RealTimeLogModuleView Grid.Row="2"
                                               DataContext="{Binding RealTimeLogModule}" />
        </Grid>

        <Border Grid.Column="2"
                Background="{DynamicResource RegionBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="10"
                Padding="4">
            <moduleViews:TestItemTreeModuleView
                                            DataContext="{Binding TestItemTreeModule}" />
        </Border>
    </Grid>
</UserControl>
